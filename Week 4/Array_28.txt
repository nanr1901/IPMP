class Solution{
public:	

	void segregateEvenOdd(int arr[], int n) {
	    int k=0;
	    for (int i=0;i<n;i++)
	    {
	        if (arr[i]%2==0) {swap(arr[i],arr[k]);k++;}
	    }
	    sort(arr,arr+k);
	    sort(arr+k,arr+n);
	}
};